from setuptools import setup, find_packages

setup(name='omelettetests.egg1',
      packages=find_packages(),
      namespace_packages=['omelettetests'],
      install_requires=['setuptools'],
      )
